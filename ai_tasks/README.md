# AI Task Files

Give one task file at a time to Cursor, Claude Code, or Codex. Always ask the agent to read `AGENTS.md`, `PROJECT_CONTEXT.md`, `ARCHITECTURE.md`, and the chosen task file before acting.

Do not run two agents on the same task or same file set concurrently.
