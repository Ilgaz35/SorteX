# TASK-001 — Bootstrap and Verification

## Goal
Verify the Python package, test setup, config loading approach, and repository hygiene without adding product logic.

## Allowed paths
- `pyproject.toml`
- `requirements.txt`
- `src/sortex/`
- `tests/`
- `docs/`

## Acceptance criteria
- `pytest` succeeds.
- `ruff check .` succeeds or any needed config is added.
- A short `docs/development_setup.md` explains Windows PowerShell setup and test commands.
- Do not create vision/control implementation yet.

## Deliverable report
Changed files, commands run/results, limitations, suggested commit message.
