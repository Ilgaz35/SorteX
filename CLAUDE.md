# Claude Code Project Memory

Follow `AGENTS.md` as the primary engineering guide. Before any implementation, read the active task file in `ai_tasks/ehm/` and preserve the contracts in `ARCHITECTURE.md`.

When reviewing or refactoring, prioritize determinism, testability, safe valve-off behavior, and clear module boundaries.
